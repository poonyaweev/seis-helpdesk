<!DOCTYPE html>
<html>
<head>
    <title>Helpdesk Tickets</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/core@1.1.1/dist/css/tabler.min.css" />
</head>
<body>
    <div class="row mb-3">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Ticket Status</h3>
          </div>
          <div class="card-body">
            <div class="row">
              <div class="col-md-3">
                <div class="card bg-primary-lt">
                  <div class="card-body">
                    <div class="d-flex align-items-center">
                      <div class="subheader">Open Tickets</div>
                      <div class="ms-auto lh-1">
                        <div class="text-muted">Open</div>
                      </div>
                    </div>
                    <div class="h1 mb-3"><%= counts.Open || 0 %></div>
                    <div class="d-flex mb-2">
                      <div>New tickets awaiting response</div>
                    </div>
                  </div>
                </div>
              </div>
              <div class="col-md-3">
                <div class="card bg-yellow-lt">
                  <div class="card-body">
                    <div class="d-flex align-items-center">
                      <div class="subheader">In Progress</div>
                      <div class="ms-auto lh-1">
                        <div class="text-muted">In Progress</div>
                      </div>
                    </div>
                    <div class="h1 mb-3"><%= counts['In Progress'] || 0 %></div>
                    <div class="d-flex mb-2">
                      <div>Tickets currently worked on</div>
                    </div>
                  </div>
                </div>
              </div>
              <div class="col-md-3">
                <div class="card bg-green-lt">
                  <div class="card-body">
                    <div class="d-flex align-items-center">
                      <div class="subheader">Resolved</div>
                      <div class="ms-auto lh-1">
                        <div class="text-muted">Resolved</div>
                      </div>
                    </div>
                    <div class="h1 mb-3"><%= counts.Resolved || 0 %></div>
                    <div class="d-flex mb-2">
                      <div>Tickets that have been resolved</div>
                    </div>
                  </div>
                </div>
              </div>
              <div class="col-md-3">
                <div class="card bg-dark-lt">
                  <div class="card-body">
                    <div class="d-flex align-items-center">
                      <div class="subheader">Closed</div>
                      <div class="ms-auto lh-1">
                        <div class="text-muted">Closed</div>
                      </div>
                    </div>
                    <div class="h1 mb-3"><%= counts.Closed || 0 %></div>
                    <div class="d-flex mb-2">
                      <div>Tickets that have been closed</div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Helpdesk Tickets</h3>
            <div class="card-actions">
              <a href="/terms" class="btn btn-primary">
                <i class="ti ti-plus"></i> Create New Ticket
              </a>
            </div>
          </div>
          <div class="card-body">
            <div class="table-responsive">
              <table class="table table-vcenter card-table table-striped">
                <thead>
                  <tr>
                    <th>Ticket Number</th>
                    <th>Menu</th>
                    <th>Description</th>
                    <th>Status</th>
                    <th>Created At</th>
                    <th>Updated At</th>
                    <th>Images</th>
                    <th class="w-1">Actions</th>
                  </tr>
                </thead>
                <tbody>
                  <% tickets.forEach(ticket => { %>
                  <tr>
                    <td class="text-secondary"><%= ticket.ticketNumber %></td>
                    <td><%= ticket.menu %></td>
                    <td><%= ticket.description %></td>
                    <td>
                      <% if (ticket.status === 'Open') { %>
                        <span class="badge bg-blue-lt"><%= ticket.status %></span>
                      <% } else if (ticket.status === 'In Progress') { %>
                        <span class="badge bg-yellow-lt"><%= ticket.status %></span>
                      <% } else if (ticket.status === 'Resolved') { %>
                        <span class="badge bg-green-lt"><%= ticket.status %></span>
                      <% } else if (ticket.status === 'Closed') { %>
                        <span class="badge bg-dark-lt"><%= ticket.status %></span>
                      <% } %>
                    </td>
                    <td><%= ticket.createdAt.toLocaleString() %></td>
                    <td><%= ticket.updatedAt.toLocaleString() %></td>
                    <td>
                      <% if (ticket.image) { %>
                        <a href="/image/<%= ticket._id %>" class="btn btn-sm btn-outline-primary">
                          <i class="ti ti-photo"></i> View
                        </a>
                      <% } else { %>
                        <span class="text-muted">No Image</span>
                      <% } %>
                    </td>
                    <td>
                      <div class="btn-list flex-nowrap">
                        <a href="/view/<%= ticket._id %>" class="btn btn-sm btn-outline-primary">
                          <i class="ti ti-eye"></i> View
                        </a>
                        <a href="/update/<%= ticket._id %>" class="btn btn-sm btn-outline-secondary">
                          <i class="ti ti-edit"></i> Update
                        </a>
                      </div>
                    </td>
                  </tr>
                  <% }); %>
                </tbody>
              </table>
            </div>
          </div>
          <div class="card-footer d-flex align-items-center">
            <p class="m-0 text-secondary">แสดงทั้งหมด <%= totalTickets %> รายการ</p>
            <div class="m-0 ms-auto">
              <a href="/export" class="btn btn-outline-primary">
                <i class="ti ti-download"></i> Export as CSV
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
</body>
<script src="https://cdn.jsdelivr.net/npm/@tabler/core@1.1.1/dist/js/tabler.min.js"></script>
</html>